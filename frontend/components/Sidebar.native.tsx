import { View, Text } from "react-native"

export default function Sidebar() {
    return (
        <View style={{backgroundColor: 'orange'}}>
            <Text>THIS IS NATIVE (MOBILE)</Text>
        </View>
    )
}